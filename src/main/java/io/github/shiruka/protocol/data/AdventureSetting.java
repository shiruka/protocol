package io.github.shiruka.protocol.data;

/**
 * an enum class that represents adventure settings.
 */
public enum AdventureSetting {
  /**
   * the world immutable.
   */
  WORLD_IMMUTABLE,
  /**
   * the no pvm.
   */
  NO_PVM,
  /**
   * the no mvp.
   */
  NO_MVP,
  /**
   * the show name tags.
   */
  SHOW_NAME_TAGS,
  /**
   * the auto jump.
   */
  AUTO_JUMP,
  /**
   * the may fly.
   */
  MAY_FLY,
  /**
   * the no clip.
   */
  NO_CLIP,
  /**
   * the world builder.
   */
  WORLD_BUILDER,
  /**
   * the flying.
   */
  FLYING,
  /**
   * the muted.
   */
  MUTED,
  /**
   * the mine.
   */
  MINE,
  /**
   * the doors and switches.
   */
  DOORS_AND_SWITCHES,
  /**
   * the open containers.
   */
  OPEN_CONTAINERS,
  /**
   * the attack players.
   */
  ATTACK_PLAYERS,
  /**
   * the attack mobs.
   */
  ATTACK_MOBS,
  /**
   * the operator.
   */
  OPERATOR,
  /**
   * the teleport.
   */
  TELEPORT,
  /**
   * the build.
   */
  BUILD,
  /**
   * the default level permissions.
   */
  DEFAULT_LEVEL_PERMISSIONS
}
